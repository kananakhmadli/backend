server:
    port: 8090
    servlet:
        context-path: /dev
management:
    endpoints:
        web:
            exposure:
                include: [ "env", "health", "info" ]
spring:
    application:
        name: ms-backend-dev
    messages:
        basename: i18n/messages
        encoding: UTF-8
    datasource:
        url: jdbc:h2:mem:testdb

logging:
    level:
        com.company: DEBUG
    file:
        name: log-dev.txt
