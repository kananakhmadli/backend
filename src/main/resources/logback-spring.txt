<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="info">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>

validation.firstNameSize.message=First name size must be between {min} and {max}
validation.firstNameNotNull.message=First name cannot be null
validation.lastNameSize.message=Last name size must be between {min} and {max}
validation.lastNameNotNull.message=Last name cannot be null
validation.emailSize.message=Email size must be between {min} and {max}
validation.emailNotNull.message=Email name cannot be null
validation.emailUniqueEmail.message=Email must be unique
datasource:
        url: jdbc:h2:mem:testdb